name: Tests

on:
  push:
    branches: [master, py3]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v7
        with:
          submodules: true

      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            gir1.2-gtk-3.0 at-spi2-core gobject-introspection \
            libgirepository1.0-dev libglib2.0-dev libcairo2-dev \
            pkg-config python3-dev intltool libgnutls28-dev xvfb

      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install Sphinx
          pip install translate-toolkit
          pip install python-Levenshtein
          pip install lxml
          pip install pycurl
          pip install diff_match_patch
          # PyGObject >=3.52 requires girepository-2.0, which ubuntu-22.04's
          # libgirepository1.0-dev doesn't provide (only girepository-1.0,
          # via gobject-introspection-1.0.pc) - pin below that.
          pip install "pygobject<3.52"
          pip install cheroot
          pip install pytest

      - name: Update translations
        run: ./maketranslations

      - name: Build docs
        run: make docs

      - name: Install virtaal
        run: python setup.py install

      - name: Compile-check all Python files
        run: find -name \*py -exec python -m py_compile {} +

      - name: Run tests
        run: xvfb-run -a --server-args="-screen 0 1024x768x24" pytest -rvxs virtaal

      - name: Smoke test the CLI
        run: xvfb-run -a --server-args="-screen 0 1024x768x24" virtaal --help
