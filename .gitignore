# /
/build
/dist
/MANIFEST
/MANIFEST.in
/mo
*.egg-info/

# /docs/
/docs/_build/

# /po/
/po/missing
/po/.intltool-merge-cache
/po/notexist

# /share/appdata/
/share/appdata/virtaal.appdata.xml

# /share/metainfo/
/share/metainfo/virtaal.metainfo.xml

# /share/applications/
/share/applications/*.desktop

# /share/mime/packages/
/share/mime/packages/*-mimetype.xml

# /share/virtaal/
/share/virtaal/virtaal-mimetype.xml
/share/virtaal/virtaal.desktop

# General
*.pyc

# gh-pages branch
_site
_layouts
assets/

#vim
.*.swp
*~

# venv
.venv

# devsupport/testing/windows/
# Ephemeral transcripts from Invoke-VirtaalLocalTestPass.ps1 - run
# records, not something to commit.
/devsupport/testing/windows/.local-test-runs/

# Generated fresh by devsupport/packaging/*/build_standalone.* right
# before a PyInstaller build, so a frozen build can report which
# commit it was actually built from (virtaal.__version__.build_commit)
# without needing a bundled .git directory or git binary. Never
# checked in - build provenance, not source.
/virtaal/_build_info.py
